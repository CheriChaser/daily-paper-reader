---
title: "StreamSplat: Streaming Feed-Forward 3D Gaussian Splatting"
title_zh: StreamSplat：流式前馈3D高斯泼溅
authors: "Changhao Song, Yuxuan Wang, Qibiao Li, Youcheng Cai, Ligang Liu"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.01659v1"
tags: ["query:dgs-recon"]
score: 9.0
evidence: 面向流式场景的前馈3DGS重建，直接匹配3DGS重建需求
tldr: 现有前馈3DGS方法依赖固定视角集联合处理，无法应对在线顺序到达的视角流。StreamSplat提出流式前馈框架，通过体素对齐因果缓存维护内存有界的持久几何状态，结合历史投影深度锚定与缓存引导特征注入，在每个输入块到达后增量解码可渲染的3D高斯。在DL3DV、RealEstate10K和ScanNet上，该方法在稀疏因果输入下与最优方法相当，并可扩展到256至1024视角的长流，质量随观测增多持续提升。为在线实时场景重建与流式渲染提供了可行方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.01659v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 882, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1814, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1833, \"height\": 1237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 874, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1829, \"height\": 476, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1822, \"height\": 1981, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.01659v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1819, \"height\": 2054, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.01659v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1848, \"height\": 894, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 887, \"height\": 542, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 376, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.01659v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 883, \"height\": 356, \"label\": \"Table\"}]"
motivation: 现有前馈3DGS方法需固定视角集并联合处理，无法应对视角顺序到达的在线场景，要求因果且内存有界地更新场景。
method: 提出StreamSplat流式框架，用体素对齐因果缓存（VACC）存储历史3D标记，通过历史投影深度锚定（HPDA）和缓存引导特征注入（CGFI）在每个输入块后增量解码可渲染的3D高斯。
result: 在DL3DV、RealEstate10K、ScanNet上稀疏因果输入下与SOTA相当，并能扩展到256至1024视角长流，质量随观测增加持续提升。
conclusion: 首次实现流式前馈3DGS，支持在线长序列输入且内存有界，为实时重建与渲染提供可扩展方案，代码将开源。
---

## 摘要
前馈3D高斯泼溅能够在无需逐场景优化的情况下实现高效的新视角合成，但大多数现有方法假设一组固定的上下文视图并联合处理它们。这限制了它们在在线场景中的适用性，在线场景中标定视图按顺序到达，且场景必须因果地更新。我们提出了StreamSplat，一个流式前馈3DGS框架，它增量地维护一个持久的、基于几何的场景状态，并在每个输入块之后将其解码为可渲染的3D高斯。StreamSplat的核心是一个体素对齐因果缓存，它将历史3D标记存储在内存受限的体素结构中，使得内存随探索的场景几何增长而不是随流长度增长。为了在因果预测中更好地重用历史，我们引入了历史投影深度锚定，将缓存的几何投影为当前代价体估计的深度指导，以及缓存引导特征注入，将缓存的潜在证据注入高斯标记回归。在DL3DV、RealEstate10K和ScanNet上的实验表明，尽管不使用未来视图或全场景上下文，StreamSplat在稀疏因果输入下仍能与最先进的前馈3DGS方法保持竞争力。更重要的是，它能扩展到具有256、512和1024个视图的长输入流，而固定视图基线在这些输入流上会耗尽内存，随着更多观测值的到达，新视角合成质量持续提升。代码将在录用后公开提供。

## Abstract
Feed-forward 3D Gaussian Splatting enables efficient novel-view synthesis without per-scene optimization, but most existing methods assume a fixed set of context views and process them jointly. This limits their applicability to online scenarios where calibrated views arrive sequentially and the scene must be updated causally. We present \emph{StreamSplat}, a streaming feed-forward 3DGS framework that incrementally maintains a persistent geometry-grounded scene state and decodes it into renderable 3D Gaussians after each input chunk. StreamSplat centers on a \textbf{Voxel-Aligned Causal Cache (VACC)}, which stores historical 3D tokens in a memory-bounded voxel structure so that memory grows with explored scene geometry rather than stream length. To better reuse history during causal prediction, we introduce \textbf{History-Projected Depth Anchoring (HPDA)} to project cached geometry as depth guidance for current cost-volume estimation, and \textbf{Cache-Guided Feature Injection (CGFI)} to inject cached latent evidence into Gaussian-token regression. Experiments on DL3DV, RealEstate10K, and ScanNet show that StreamSplat remains competitive with state-of-the-art feed-forward 3DGS methods under sparse causal inputs, despite not using future views or full-scene context. More importantly, it scales to long input streams with 256, 512, and 1024 views where fixed-view baselines run out of memory, yielding sustained improvements in novel-view synthesis quality as more observations arrive. The code will be made publicly available upon acceptance.

---

## 论文详细总结（自动生成）

# StreamSplat：流式前馈3D高斯泼溅——论文详细总结

## 1. 论文的核心问题与整体含义

- **研究背景**：前馈3D高斯泼溅（Feed-forward 3DGS）方法能够在不进行逐场景优化的情况下实现高效的新视角合成，是近年来神经渲染领域的重要进展。
- **核心问题**：现有前馈3DGS方法几乎都假设输入是**一组固定的上下文视图**，需要联合处理全部输入。这导致两个关键限制：
  - 无法处理**在线流式场景**——即标定视角按顺序依次到达、场景状态需要因果更新的真实应用场景（如机器人导航、实时摄影、自动驾驶等）。
  - 视图数量受限于显存和算力，无法扩展到长输入流（如数百至上千帧）。
- **整体含义**：论文首次将"流式"（streaming）范式引入前馈3DGS，使得场景重建可以在线、增量地进行，内存消耗有界且不随流长度增长，为实时重建与流式渲染开辟了新的技术路径。

## 2. 方法论：核心思想与关键技术

### 核心思想
- 将前馈3DGS从"固定视角集的联合处理"转变为**"递进的因果增量解码"**：维护一个持久的、基于几何的场景状态（即历史3D标记的缓存），每个输入块到达后更新该状态，并解码出可渲染的3D高斯。
- 关键权衡：在**因果性**（不使用未来视图）和**全场景上下文的缺失**下，依然实现与SOTA相当的渲染质量。

### 三类输入的组织方式
- 输入被划分为**块（chunk）**（例如固定大小的多视角批次），每当一个新块到达时执行一次"更新+解码"的增量流程，从而将无限长流的处理转化为有限块序列的重复操作。

### 三大核心技术组件

1. **体素对齐因果缓存（Voxel-Aligned Causal Cache, VACC）**
   - 将历史3D标记存储在一个**体素结构**中，体素与场景几何对齐。
   - 关键特性：内存消耗与**探索过的场景几何量**成正比，而非与流长度成正比，从而实现内存有界（memory-bounded）。
   - 新到达的3D标记通过哈希或体素索引插入缓存，已探索区域之外的新几何逐步扩展体素结构。

2. **历史投影深度锚定（History-Projected Depth Anchoring, HPDA）**
   - 将缓存中的历史几何投影到当前新视角上，生成**深度先验/指导**。
   - 该深度指导用于当前代价体（cost volume）的估计，帮助模型在当前帧中更准确地定位几何，缓解因果设置下缺乏未来信息带来的深度模糊。

3. **缓存引导特征注入（Cache-Guided Feature Injection, CGFI）**
   - 将缓存的潜在特征/证据注入到当前高斯标记（Gaussian token）的回归过程中。
   - 使当前解码时能够充分利用历史观测中已编码的场景信息，实现更好的外观与几何重建。

### 工作流程（文字描述）
1. 输入第 *t* 个视角块 → 提取图像特征。
2. 使用HPDA将当前VACC中的历史几何投影为深度锚定，辅助代价体构建与深度估计。
3. 生成新的3D标记 → 通过VACC进行哈希插入（冲突检测与更新）。
4. 从更新后的VACC查询聚合上下文特征，通过CGFI注入至高斯标记回归中。
5. 解码渲染当前可用的3D高斯，输出新视角合成结果；等待下一个输入块到达后重复上述流程。

## 3. 实验设计

### 数据集与基准
- **DL3DV**：大规模真实世界视频数据集，包含多种场景类型，是前馈3DGS的重要基准。
- **RealEstate10K**：大规模室内外房产视频数据集，常用于评估新视角合成。
- **ScanNet**：RGB-D室内扫描数据集，用于评估在真实室内场景上的泛化能力。

### 对比方法
- **输入设置**：与最先进的前馈3DGS方法在**稀疏因果输入**下进行对比（即仅使用过去视角，不使用未来视角）。
- **固定视图基线**：如MVSplat、pixelSplat等前馈3DGS方法（它们需要固定数量上下文视图联合处理）。
- **长流扩展**：在256、512和1024个视图的长输入流上验证扩展性；在这些长度下固定视图基线会**耗尽显存**。

### 评测指标
- 新视角合成质量指标，包括PSNR、SSIM、LPIPS等（按3DGS领域惯例）。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量及训练时长等具体算力信息。
- 论文只提及"固定视图基线在长流上会耗尽内存"，侧面说明StreamSplat的显存占用更低，但未给出定量的显存对比数据。
- 这是论文在可复现性报告上的一个不足之处。

## 5. 实验数量与充分性

### 实验数量
- **主实验**：3个数据集（DL3DV、RealEstate10K、ScanNet）上的稀疏因果输入对比。
- **扩展性实验**：256/512/1024视角长流输入实验。
- **消融实验**：包含对VACC、HPDA、CGFI三大组件的消融（从论文图表中可见消融表格）。

### 充分性与客观性评估
- **优点**：
  - 覆盖了室内（ScanNet）与室外/大规模场景（DL3DV、RealEstate10K），具有一定多样性。
  - 稀疏因果输入与SOTA对比，验证了方法在受限信息下的竞争力。
  - 长流扩展实验展示了方法的核心优势（可扩展性），与固定视图基线形成了清晰对比。
- **不足**：
  - 未与其他流式重建方法（如流式NeRF/3DGS方法）直接对比，对比范围主要局限在前馈3DGS领域。
  - 长流实验缺少更多的定量质量曲线细节（如逐块PSNR变化曲线）。
  - 消融实验的具体数量与详细结果在文字部分未展开，依赖表格呈现。

## 6. 主要结论与发现

- **因果稀疏输入下的竞争力**：尽管不使用未来视图或全场景上下文，StreamSplat在DL3DV、RealEstate10K和ScanNet上与最先进的前馈3DGS方法保持竞争力。
- **长流可扩展性**：能处理256、512乃至1024个视图的长输入流，而固定视图基线在此规模下显存耗尽——证明了内存有界设计的有效性。
- **质量持续提升**：随着更多观测的到达，新视角合成质量呈现**持续改善**趋势，说明历史缓存的使用是有效的、信息不断累积。
- **核心价值**：首次验证了"流式前馈3DGS"这一范式的可行性，为在线实时重建与流式渲染提供了可扩展的解决方案。

## 7. 优点

- **问题选择具有前瞻性**：面向在线/流式场景的研究与机器人、自动驾驶、AR/VR等实际应用的实时重建需求直接相关。
- **方法论设计巧妙**：
  - VACC的内存有界设计从原理上解决了长流处理的瓶颈，性质优雅。
  - HPDA和CGFI两个模块分别从"几何引导"和"特征复用"两个互补角度解决了因果预测中的历史重用问题。
- **实验验证了核心主张**：长流可扩展性和质量随观测增加而提升，直接证实了方法的独特贡献。
- **适用范围广**：适用于三个不同风格的数据集，表明方法具有一定泛化能力。

## 8. 不足与局限

- **算力信息公开不足**：未报告GPU型号、数量、训练时间等训练成本信息，影响可复现性与工程参考价值。
- **对比范围偏窄**：未与其他流式重建方法（如增量NeRF/流式3DGS相关工作）进行对比，当前对比主要局限于固定视图前馈方法，缺乏对"流式"这一维度的同类比较。
- **长流实验细节有限**：未充分展示长流输入下渲染质量随块数变化的详细曲线，以及缓存大小/显存占用的定量分析。
- **场景尺度与类别有限**：DL3DV虽包含多种场景，但整体仍以中小规模场景为主；缺乏大规模户外场景（如自动驾驶数据集）的验证。
- **因果假设的局限**：方法假设标定视图按顺序到达且姿态已知，未讨论姿态估计误差对性能的影响；在实际在线系统中姿态可能含噪声。
- **视觉质量上限**：由于不使用未来视图，理论上在稀疏视角下重建质量的提升存在上限；文中也未对比"使用全上下文"的绝对上界。

（完）
