---
title: "3D Gaussian Accelerated Ray Tracing: Fast training through particle-based backward propagation"
title_zh: 三维高斯加速光线追踪：通过基于粒子的反向传播实现快速训练
authors: "Laurent Vit, Oliver Batchelor, Richard Green"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17298v1"
tags: ["query:dgs-recon"]
score: 8.0
evidence: 通过基于粒子的反向传播加速3D高斯光线追踪训练
tldr: 3D高斯泼溅的光栅化近似限制了反射、阴影等效果，光线追踪虽提升质量但训练缓慢。3DGART识别瓶颈不在光线遍历而在像素级反向传播的原子竞争，提出围绕primitive重组梯度计算，利用保守屏幕空间边界构建tile映射，将高竞争scatter转为gather。在Mip-NeRF 360上训练提速约3.5倍，比3DGRT快4倍，同时质量提升。这使得全光线追踪高斯训练实用，运行时间可与光栅化管线匹敌，并保留光线追踪优势。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17298v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 847}, {\"url\": \"assets/figures/arxiv/2608.17298v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 668, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2608.17298v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 886, \"height\": 484}, {\"url\": \"assets/figures/arxiv/2608.17298v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 870, \"height\": 767}, {\"url\": \"assets/figures/arxiv/2608.17298v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 856, \"height\": 534}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.17298v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 909, \"height\": 239}, {\"url\": \"assets/tables/arxiv/2608.17298v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1894, \"height\": 626}]"
motivation: 高斯光线追踪的训练瓶颈在于像素为中心的梯度后向传播中的原子竞争与串行化，而非光线遍历本身。
method: 围绕primitive重组反向传播，使用保守屏幕空间边界构建tile映射，将梯度计算从scatter操作转为gather。
result: 在Mip-NeRF 360上训练比per-pixel基线快约3-3.5倍，比3DGRT快约4倍，并提升渲染质量。
conclusion: 3DGART使全光线追踪高斯训练实际可行，速度接近光栅化管线同时保留光线追踪优势。
---

## 摘要
三维高斯泼溅使高斯基元成为一种用于实时新视角合成的高效表示，但其基于光栅化的公式依赖于屏幕空间近似，这限制了精确的视角相关排序以及反射、折射和阴影等次级光线效果的集成。高斯光线追踪通过评估显式的光线-基元相交来解决这些限制，但其训练成本仍然高昂。我们观察到主要瓶颈不仅在于光线遍历本身，还在于以像素为中心的反向传播，其中许多线程同时向相同基元参数累积梯度，导致严重的原子争用和线程串行化。我们提出了3DGART，一个用于光线追踪高斯渲染的实用训练框架。我们的关键思想是将反向传播围绕基元而非像素进行重组。利用保守的透视校正屏幕空间边界，我们构建了一个紧凑的中间缓冲区和瓦片-基元映射，允许每个线程在一个瓦片内累积一个基元对其覆盖像素的贡献。这将梯度计算从争用密集的分散操作转变为结构化的类聚集过程。在Mip-NeRF 360上，3DGART相较于逐像素基线实现了约3–3.5倍的原始训练加速，相较于3DGRT在Mip-NeRF 360上实现了约4倍的加速，同时提高了质量。更重要的是，3DGART使完全光线追踪的高斯训练变得实用，达到与基于光栅化流程相当的运行时间，同时保留了光线追踪的优势。

## Abstract
3D Gaussian Splatting has made Gaussian primitives a highly efficient representation for real-time novel view synthesis, but its rasterisation-based formulation relies on screen-space approximations that limit accurate view-dependent ordering and the integration of secondary ray effects such as reflections, refractions, and shadows. Gaussian ray tracing addresses these limitations by evaluating explicit ray-primitive intersections, yet it remains costly to train. We observe that the main bottleneck is not ray traversal alone, but the pixel-centric backward propagation, where many threads concurrently accumulate gradients into the same primitive parameters, causing severe atomic contention and thread serialisation.   We present 3DGART, a practical training framework for ray-traced Gaussian rendering. Our key idea is to reorganise backward propagation around primitives rather than pixels. Using conservative perspective-correct screen-space bounds, we build a compact intermediate buffer and a tile-primitive mapping that allows each thread to accumulate the contribution of one primitive over its covered pixels within a tile. This transforms gradient computation from a contention-heavy scatter operation into a structured gather-like process. On Mip-NeRF 360, 3DGART achieves an $\approx 3-3.5\times$ raw training speedup over per-pixel baseline and $\approx4 \times$ over 3DGRT on Mip-NeRF 360 while improving quality. More importantly, 3DGART makes fully ray-traced Gaussian training practical, reaching runtimes competitive with rasterisation-based pipelines while preserving benefits of ray tracing.

---

## 论文详细总结（自动生成）

## 三维高斯加速光线追踪（3DGART）论文总结

### 1. 核心问题与整体含义

- **背景**：3D Gaussian Splatting（3DGS）以高斯基元为基础，实现了实时新视角合成。但其光栅化流程依赖屏幕空间近似，无法精确处理视角相关的排序，也难以集成反射、折射、阴影等次级光线效果。
- **问题**：高斯光线追踪（Gaussian ray tracing）通过显式的光线-基元求交解决了上述限制，但训练成本极高，限制了其实际应用。
- **关键洞察**：作者发现训练慢的瓶颈并不主要在光线遍历（ray traversal）本身，而在于**以像素为中心的反向传播**——大量线程同时向同一基元参数累积梯度，导致严重的**原子竞争（atomic contention）**和线程串行化。
- **整体意义**：3DGART 通过重组梯度计算方式，使全光线追踪的高斯训练速度接近光栅化管线，首次让“高质量光线追踪 + 高斯表示”的组合在训练效率上变得实际可行。

### 2. 方法论

- **核心思想**：将反向传播从“以像素为中心”重组为“以基元（primitive）为中心”，从而避免多线程对相同参数的高争用写入。
- **关键技术细节**：
  - 为每个基元计算**保守的透视校正屏幕空间边界**（conservative perspective-correct screen-space bounds），确定其可能覆盖的像素范围。
  - 基于这些边界构建一个**紧凑的中间缓冲区**和 **tile-基元映射**（tile-primitive mapping）。
  - 每个线程负责在某一个 tile 内累积**一个基元**对其覆盖像素的梯度贡献，将梯度计算从高争用的 **scatter（分散）** 操作转换为结构化的 **gather（聚集）** 过程。
- **算法流程（文字描述）**：
  1. 前向：执行光线-基元求交与渲染，生成图像。
  2. 反向准备：为每个基元计算保守屏幕空间边界，构建 tile 映射。
  3. 反向传播：按基元-线程的组织方式，每个线程独立处理一个基元在一个 tile 内的所有像素梯度，按序累加，避免原子竞争。
  4. 汇总梯度：将各基元的梯度汇总用于参数更新。

### 3. 实验设计

- **数据集/场景**：使用 **Mip-NeRF 360** 数据集作为主要 benchmark。
- **对比方法**：
  - **逐像素（per-pixel）基线**：即以像素为中心进行反向传播的光线追踪高斯训练方法。
  - **3DGRT**：现有的高斯光线追踪训练框架。
- **评估指标**：训练原始速度（raw training speedup）以及渲染质量（PSNR 等，摘要中提及“同时提高了质量”）。

### 4. 资源与算力

- **论文未明确说明**所使用的 GPU 型号、数量及具体训练时长。
- 仅在摘要中报告了相对加速倍数（3–3.5× vs per-pixel 基线，≈4× vs 3DGRT），没有透露绝对训练时间或硬件配置。

### 5. 实验数量与充分性

- **实验数量**：摘要中只报告了在 Mip-NeRF 360 上的训练加速与质量对比，未提及消融实验（如 tile 大小、边界保守程度的影响）或多数据集验证。
- **充分性评估**：
  - 该实验设计能直接验证核心主张（加速与质量），但作为一篇完整论文，**实验覆盖面偏窄**——仅一个数据集、仅两个对比基线。
  - 缺乏对反射/折射等光线追踪特有优势场景的定性或定量展示。
  - 缺少消融分析来证明各设计组件（保守边界、tile 映射、gather 策略）各自的贡献。
  - 总体而言，初步结果有说服力，但充分性有待补充更多实验支撑。

### 6. 主要结论与发现

- 在高斯光线追踪训练中，主要瓶颈是像素级反向传播中的**原子竞争与线程串行化**，而非光线遍历本身。
- 通过围绕基元重组反向传播、利用保守屏幕空间边界构建 tile 映射，可将梯度计算从高争用的 scatter 转为结构化 gather，显著降低竞争开销。
- 在 Mip-NeRF 360 上，3DGART 相较逐像素基线实现约 **3–3.5 倍**原始训练加速，相较 3DGRT 实现约 **4 倍**加速，同时**提升了渲染质量**。
- 核心意义在于：**完全光线追踪的高斯训练可以变得实用**，其运行时间可与光栅化管线竞争，同时保留了光线追踪的影像质量优势。

### 7. 优点

- **问题诊断精准**：将瓶颈定位到原子竞争而非光线遍历，具有明确的针对性。
- **方法优雅、工程性强**：利用保守屏幕空间边界构建 tile 映射，将 scatter 转为 gather，实现简单且高效。
- **结果显著**：3–4 倍的训练加速且质量提升，改善幅度大。
- **实用性高**：让光线追踪高斯训练首次达到与光栅化流程可比的运行时间，拓展了实用性边界。

### 8. 不足与局限

- **实验覆盖有限**：仅报告了 Mip-NeRF 360 一个数据集，未在多场景、多分辨率、室内/室外等多样化数据上验证。
- **缺乏消融实验**：未说明各设计组件（保守边界、tile 映射、gather 重组）的独立贡献。
- **未报告算力信息**：没有 GPU 型号、数量、绝对训练时长等关键复现信息。
- **未涵盖光线追踪优势场景**：缺少对反射、折射、阴影等效果的定性对比，无法全面衡量相对光栅化的质量收益。
- **未讨论局限性**：如 tile 边界过保守时的额外开销、极端场景（大量细小基元）下的表现、内存占用变化等均未提及。

（完）
