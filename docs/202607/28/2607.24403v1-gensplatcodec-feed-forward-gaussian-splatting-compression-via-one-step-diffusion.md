---
title: "GenSplatCodec: Feed-Forward Gaussian Splatting Compression via One-Step Diffusion"
title_zh: "GenSplatCodec: 基于单步扩散的前馈高斯泼溅压缩"
authors: "Qiang Hu, Zhenlong Wu, Lei Huang, Zihan Zheng, Xiaoyun Zhang, Wenjun Zhang"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24403v1"
tags: ["query:dgs-recon"]
score: 9.0
evidence: 前馈3D高斯泼溅压缩，用于重建场景
tldr: 前馈3D高斯泼溅生成密集高斯导致存储传输成本高，现有压缩方法在低比特率下因丢弃高频纹理和视图依赖外观而失效。GenSplatCodec提出双流编码（紧凑结构流+轻量参考外观流）和几何引导的一步生成式解码，通过分层几何控制联合重建高保真视图一致新视图。多数据集实验证明其在率失真性能上显著优于已有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24403v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1871, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24403v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 918, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24403v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1871, \"height\": 488, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24403v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1851, \"height\": 1155, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24403v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1888, \"height\": 983, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24403v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 921, \"height\": 676, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24403v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 923, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24403v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 919, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24403v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 876, \"height\": 364, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24403v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1682, \"height\": 468, \"label\": \"Table\"}]"
motivation: 现有前馈高斯压缩在低比特率下丢失高频细节与视图相关外观，生成式后处理破坏跨视图一致性，亟需统一编解码架构。
method: 提出GenSplatCodec，采用双流编码（结构流+外观流）和几何引导的一步生成式解码，通过分层几何控制实现高质量重建。
result: 在多个数据集上，GenSplatCodec的率失真性能一致优于现有方法，生成高保真且视图一致的新视图。
conclusion: GenSplatCodec通过几何引导生成式解码统一前馈高斯编解码，在低比特率下实现高效压缩与高质量重建。
---

## 摘要
前馈3D高斯泼溅（3DGS）无需逐场景优化即可实现可扩展的场景重建，但会产生密集的高斯体，存储和传输成本高昂。现有前馈高斯压缩方法将解码视为确定性表示恢复，在低频比特率下，当高频纹理和视角相关外观被丢弃时，这种方法变得不充分。尽管生成模型提供了一种有希望的替代方案，但将其作为独立的后处理会解耦生成与传输的场景结构，从而损害跨视图一致性。为解决这些限制，我们提出了GenSplatCodec，一个统一的前馈高斯编解码器，将低比特率高斯压缩重新定义为几何引导的生成解码。我们在双流公式中提出了一种细节感知的前馈高斯编码方案，其中紧凑的高斯结构流由轻量级的参考外观流补充。我们进一步引入了一种几何引导的单步生成解码方法，通过分层几何控制联合利用解码后的结构和外观线索，以重建高保真且视图一致的新视图。最后，我们开发了一种三阶段优化策略，稳定统一编解码器的学习，并使生成解码器适应编解码器派生的结构和外观线索。跨多个数据集的广泛实验表明，GenSplatCodec在率失真（RD）性能上始终优于现有方法。

## Abstract
Feed-forward 3D Gaussian Splatting (3DGS) enables scalable scene reconstruction without per-scene optimization, yet produces dense Gaussians that are costly to store and transmit. Existing feed-forward Gaussian compression methods formulate decoding as deterministic representation recovery, which becomes inadequate at low bitrates when high-frequency textures and view-dependent appearance are discarded. Although generative models offer a promising alternative, using them as standalone post-processing decouples generation from the transmitted scene structure, thereby compromising cross-view consistency. To address these limitations, we propose GenSplatCodec, a unified feed-forward Gaussian codec that reformulates low-bitrate Gaussian compression as geometry-guided generative decoding. We present a detail-aware feed-forward Gaussian coding scheme within a dual-stream formulation, where the resulting compact Gaussian structural stream is complemented by a lightweight reference appearance stream. We further introduce a geometry-guided one-step generative decoding approach that jointly exploits decoded structural and appearance cues through hierarchical geometry control to reconstruct high-fidelity and view-consistent novel views. Finally, we develop a three-stage optimization strategy that stabilizes the learning of the unified codec and adapts the generative decoder to codec-derived structural and appearance cues. Extensive experiments across multiple datasets demonstrate that GenSplatCodec consistently achieves superior rate-distortion (RD) performance over existing methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：前馈3D高斯泼溅（3DGS）无需逐场景优化即可快速重建，但产生密集的高斯体，存储和传输成本过高。现有前馈高斯压缩方法采用确定性解码，在低比特率下会丢失高频纹理和视图相关外观，导致渲染质量严重退化。简单地在解码后添加生成式后处理虽然能恢复细节，但生成过程与传输的场景结构解耦，容易产生跨视图不一致的伪影。
- **核心问题**：如何设计一个统一的前馈高斯编解码系统，在极低比特率下既保留可靠的结构信息，又能通过生成式解码恢复缺失的细节，同时保持跨视图一致性。
- **整体含义**：本文提出将低比特率压缩重构为“几何引导的生成式解码”问题，将紧凑的结构编码与轻量的参考外观编码相结合，并通过单步扩散模型生成高质量新视图，从而突破传统确定性压缩在低码率下的性能瓶颈。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用双流编码（高斯结构流 + 参考外观流）和单步生成式解码。高斯流保留可靠的3D结构、可见性和粗粒度外观；参考流提供难以在紧凑高斯中保留的高频/视图相关外观线索。解码时利用解码后的结构锚点和外观线索，通过分层几何控制实现跨视图外观转移，单步生成最终视图。
- **关键技术细节**：
  - **细节感知的前馈高斯编码**：
    - 双分支高斯预测：结构分支（局部-全局注意力）推理3D结构，细节分支（DINO特征+轻量MLP）保留细粒度外观，仅在属性预测头注入细节特征。
    - 率失真引导的高斯压缩：为每个高斯计算视觉贡献分 \(v_j\) 与估计属性编码代价 \(b_{a_j}\) 的比值 \(p_j^{\text{RD}} = v_j / (b_{a_j} + \epsilon)\)，保留比值高的高斯，去除冗余。
    - 几何-属性解耦编码：高斯中心用G-PCC压缩为位流 \(B_\mu\)；属性（颜色、透明度、缩放、旋转）经分析变换、量化、层次熵模型（超先验+空间上下文+通道自回归）压缩为 \(B_m, B_h\)。
  - **轻量参考外观流**：选择与查询视图最接近的参考视图，用VAE编码器提取潜在，量化后经熵编码得到 \(B_R\)，比特率计入总率。
  - **几何引导的单步生成解码**：
    - 从高斯流解码出紧凑高斯 \(\hat{G}\)，渲染出查询视图RGB锚点 \(\hat{I}_q^g\)、查询深度图 \(\hat{D}_q^g\)、参考深度图 \(\hat{D}_r^g\)。
    - 深度图经归一化和空间重采样得到几何嵌入 \(g_q, g_r\)，由轻量几何控制器生成多级控制信号 \(\{\Delta H_v^\ell\}\)。
    - 查询分支：用VAE编码器将 \(\hat{I}_q^g\) 映射为潜在 \(z_q\)；参考分支：直接使用解码出的 \(\hat{z}_r\)。二者输入共享的单步多视图U-Net，在每个阶段注入几何控制信号，并通过交叉注意力实现跨视图潜在交互。输出查询潜在经VAE解码器得到最终图像 \(\tilde{I}_q\)。
  - **三阶段优化**：
    - Stage 1：预训练前馈高斯预测网络（无压缩约束），使用MSE、LPIPS、不透明度正则化损失。
    - Stage 2：冻结高斯预测器，优化率失真引导压缩和属性熵模型，损失函数包含渲染保真度（MSE+SSIM）和归一化码率估计。
    - Stage 3：冻结整个高斯编解码路径，训练几何控制器和单步U-Net，适应实际编码后的结构和外观条件，损失为像素域MSE+LPIPS。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **DL3DV**：包含室内外多样化场景，评估分辨率 224×224 和 518×518，使用2、4、6个上下文视图。
  - **RealEstate10K (RE10K)**：主要为室内住宅场景，标准2视图协议，分辨率 224×224。
- **基准**：无场景特定优化，采用官方测试集。
- **对比方法**：
  - **前馈高斯重建方法**：DepthSplat、AnySplat、YoNoSplat（作为无压缩参考）。
  - **级联压缩基线**：将上述方法预测的高斯用 FCGS（λ=0.0001）或 SOGS（默认配置）压缩。表中记为“A+B”。
- **评价指标**：
  - 保真度：PSNR、SSIM。
  - 感知质量：LPIPS、FID、MUSIQ、MANIQA。
  - 压缩效率：每场景平均总位流大小（MB），包括高斯流和参考流。

### 4. 资源与算力

- 文中**未明确说明**使用的GPU型号、数量或训练时长。仅在运行时分析中给出单场景编码1.58秒、解码1.80秒（含生成0.12秒），但未提供训练资源信息。
- 需要指出：论文未披露训练硬件配置，这降低了结果的可复现性评估。

### 5. 实验数量与充分性

- **实验组数**：非常充分。包括：
  - 主表（Table I）在DL3DV和RE10K两个数据集上对比多种方法，三个码率点（Low/Mid/High）。
  - 率失真曲线（Fig. 3）直观展示RD性能。
  - 不同输入视图数实验（Table II：2/4/6视图）。
  - 多视图一致性量化（Table III：SIFT、CLIP、LPIPS、VBench指标）。
  - 高分辨率评估（Table IV：518×518）。
  - 消融研究（Table V）：逐步累加各组件。
  - 比特分配分析（Table VI）。
  - 运行时分析。
- **充分性与公平性**：
  - 消融实验设计合理，从基础模型逐步添加编码方案、压缩、生成解码等，每步都报告关键指标。
  - 对比方法选取了主流前馈方法和最新压缩器（FCGS、SOGS），覆盖了有/无压缩的情况。
  - 使用多视图数和高分辨率验证鲁棒性。
  - 多视图一致性对比了MVSplat360、LatentSplat等专门做一致性的方法，较为全面。
  - 但存在潜在偏差：级联压缩基线使用公开模型默认参数，可能未对每个前馈方法最优调参；论文中FCGS和SOGS仅用一种配置，可能未穷尽最佳压缩点。

### 6. 论文的主要结论与发现

- **主要结论**：GenSplatCodec在极低比特率下显著优于现有方法。
  - 在DL3DV上，Ours-Mid与YoNoSplat PSNR相当（20.02 vs 20.09 dB），但存储从6.508 MB降至0.376 MB（17.3×压缩），且SSIM、LPIPS、FID更优。
  - Ours-High在所有指标上达到最佳，仅0.830 MB。
  - 在RE10K上，Ours-High PSNR最高（23.33 dB），LPIPS最低（0.125），存储仅为0.748 MB（相比YoNoSplat的6.508 MB降低88.5%）。
  - 多视图一致性指标领先（SIFT 90.88 vs 78.90）。
  - 消融表明：双流编码、率失真压缩、几何引导SD均贡献显著；生成解码恢复细节而不增加传输开销。
- **发现**：将压缩重新定义为几何引导的生成式解码，比单纯压缩确定性表示或后处理生成更有效。紧凑结构流提供可靠锚点，参考外观流补充关键信息，分层几何控制保证跨视图一致性。

### 7. 优点：方法或实验设计上的亮点

- **方法上**：
  - 首次将前馈高斯压缩与生成式解码统一在编解码框架内，而非后处理。
  - 双流设计巧妙分离结构编码和细节生成，参考流仅占极小比率（<3%）。
  - 单步生成解码避免迭代采样，推理高效。
  - 三阶段优化策略稳定且适应实际编码条件。
- **实验上**：
  - 多数据集、多分辨率、多输入视图数的全面评估。
  - 多视图一致性量化（SIFT、CLIP、VBench）是亮点，验证了几何控制的有效性。
  - 率失真曲线和消融实验设计严谨。

### 8. 不足与局限

- **实验覆盖**：
  - 未在大规模场景（如城市级）或动态场景上评估。
  - 未与其他基于扩散的后处理方法（如直接对渲染图进行超分/增强）进行比较。
  - 级联压缩基线可能未优化参数，公平性存疑。
- **偏差风险**：
  - 参考视图选择简单依赖相机距离，未考虑遮挡或光照差异。
  - 训练数据来自特定数据集，泛化到未知场景或极端光照/纹理可能存在风险。
- **应用限制**：
  - 需要稀疏输入视图（≥2），不适用于单视图重建。
  - 生成式解码依赖大规模预训练VAE和U-Net，模型参数较大，端侧部署困难。
  - 未报告训练GPU资源与时间，影响可复现性。
- **理论局限**：论文未深入分析率失真界限或生成式解码的理论最优性。

（完）
