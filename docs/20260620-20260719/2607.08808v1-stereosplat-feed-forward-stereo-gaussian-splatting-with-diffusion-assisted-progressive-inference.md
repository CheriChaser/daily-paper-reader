---
title: "StereoSplat+: Feed-Forward Stereo Gaussian Splatting with Diffusion-Assisted Progressive Inference"
title_zh: "StereoSplat+: 前馈立体高斯泼溅与扩散辅助渐进推理"
authors: "Zihua Liu, Masatoshi Okutomi"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08808v1"
tags: ["query:dgs-recon"]
score: 9.0
evidence: 前馈立体高斯泼溅，扩散辅助渐进推理
tldr: 现有3DGS方法需多视角观测，不适合设备端应用。提出StereoSplat+，含StereoSplat前馈估计器和扩散增强渐进推理。在KITTI-360上遮挡和强外推下提升渲染质量与几何精度。实现从单一立体对的高质量因果重建。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08808v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1829, \"height\": 813, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 890, \"height\": 251, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1713, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1722, \"height\": 462, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 879, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08808v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 875, \"height\": 523, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08808v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1741, \"height\": 412, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08808v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 869, \"height\": 377, \"label\": \"Table\"}]"
motivation: 现有3DGS依赖多视角或未来帧，单一立体对存在遮挡和缺失几何，限制了设备端应用。
method: 构建输入不变的前馈3DGS估计器StereoSplat，融合代价体和三平面分支；扩散增强渐进推理扩增立体观测。
result: 在KITTI-360上，StereoSplat+在遮挡区域和强外推下显著提升新视图渲染和几何精度。
conclusion: 提出首个扩散增强的前馈立体GS框架，实现从单次立体对的因果高质量重建。
---

## 摘要
近期3D高斯泼溅（3DGS）的进展使得高质量、可渲染的场景表示用于新视角合成成为可能。然而，大多数现有3DGS流程依赖于多视角观测（或对未来帧的非因果访问）以实现充分覆盖，这在设备端机器人和增强现实设置中通常不可行，因为感知仅限于单个立体相机。因此，从单个立体观测中恢复高质量的3DGS场景仍然具有挑战性，原因是遮挡、视野有限以及几何缺失。我们提出了StereoSplat+，一个扩散增强的前馈框架，能够从单个立体对实现因果重建。我们的方法基于两个关键组件。首先，我们提出StereoSplat，一个输入不变的前馈3D高斯估计器，它以可变数量的带位姿立体对作为输入，并预测高质量的3D高斯。StereoSplat通过代价体分支和三平面基3D体积分支融合互补的几何线索，并利用连续位姿编码来泛化到不同视角数量和相机配置。其次，由于推理时通常无法获得多个带位姿的立体对，我们引入了一个扩散增强的单次渐进推理方案，称为StereoSplat+：从一个立体对开始，我们从预测的3DGS渲染新的立体视图，使用单步扩散增强器对其进行细化，并将其作为额外输入反馈以更新3DGS。在KITTI-360数据集上的实验表明，StereoSplat+在新视角渲染质量和几何精度上均有提升，特别是在遮挡区域和强外推视角下，优于近期前馈3DGS基线方法。

## 速览
**TLDR**：针对单视点观测无法获得高质量3DGS场景的问题，提出StereoSplat+框架。核心包括：一是StereoSplat前馈模型，可处理任意数量立体对并融合几何线索；二是扩散增强渐进推理，从单立体对开始，渲染新视角并用扩散模型优化后反馈迭代。在KITTI-360上，该方法在遮挡区域和强外推下显著提升新视图渲染质量与几何精度，优于现有前馈基线。 \
**Motivation**：现有3DGS依赖多视角观测，单立体对输入无法覆盖遮挡和缺失区域。 \
**Method**：提出StereoSplat前馈高斯估计器与扩散增强渐进推理方案，从单立体对迭代生成并优化新视角。 \
**Result**：在KITTI-360上，新视图渲染质量和几何精度优于前馈基线，尤其在遮挡和强外推场景。 \
**Conclusion**：StereoSplat+实现了从单立体对因果重建高质量3DGS，支持实时应用。

---

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) have enabled high-quality, render-ready scene representations for novel-view synthesis. However, most existing 3DGS pipelines rely on multi-view observations (or non-causal access to future frames) to achieve sufficient coverage, which is often unavailable in on-device robotics and AR settings where sensing is restricted to a single stereo rig. Recovering a high-quality 3DGS scene from one stereo observation, therefore, remains challenging due to occlusions, limited field of view, and missing geometry. We present StereoSplat+, a diffusion-enhanced feed-forward framework that enables causal reconstruction from a single stereo pair. Our method builds on two key components. First, we propose StereoSplat, an input-invariant feed-forward 3D Gaussian estimator that takes a variable number of posed stereo pairs as input and predicts high-quality 3D Gaussians. StereoSplat fuses complementary geometry cues via a cost-volume branch and a triplane-based 3D volume branch and leverages continuous pose encoding to generalize across view counts and camera configurations. Second, since multiple posed stereo pairs are typically unavailable at inference time, we introduce a diffusion-enhanced one-shot progressive inference scheme called StereoSplat+: starting from one stereo pair, we render novel stereo views from the predicted 3DGS, refine them with a one-step diffusion enhancer, and feed them back as additional inputs to update the 3DGS. Experiments on the KITTI-360 dataset show that StereoSplat+ improves novel-view rendering quality and geometry accuracy, especially in occluded regions and under strong view extrapolation, outperforming recent feed-forward 3DGS baselines.

---

## 论文详细总结（自动生成）

# StereoSplat+ 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
现有前馈3D高斯泼溅（3DGS）方法通常需要多视角观测或对未来帧的非因果访问，以获取足够的场景覆盖和几何约束。然而，在机器人、车载和AR等设备端应用中，传感器通常只限于单个立体相机，输入仅为单一立体对。从单个立体对重建高质量3DGS面临严重遮挡、有限视场和缺失几何区域等挑战。现有前馈方法在此设定下无法可靠地合成新视角，产生伪影和几何失真。因此，论文目标是实现从单一立体对的**因果**、**高质量**3DGS重建，兼顾实时性和泛化性。

## 2. 方法论：核心思想、关键技术细节
- **整体框架**：StereoSplat+ 包含两个核心组件：
  - **StereoSplat**：输入不变的前馈3DGS估计器，可处理可变数量（1～K）的带位姿立体对，输出3D高斯参数。
  - **StereoSplat+ 渐进推理**：从单一立体对开始，使用StereoSplat预测初始3DGS，渲染伪新视角，经一步扩散增强器优化后，作为额外输入重新送入StereoSplat更新3DGS，最后置信度加权融合两阶段结果。
- **StereoSplat 双分支架构**：
  - **代价体分支**：利用预训练的DepthAnythingV2提取几何和图像特征，通过多视图Transformer聚合，构建基于位姿距离的输入不变代价体，预测逐像素深度和3DGS参数（均值、不透明度、颜色、置信度）。
  - **3D体积分支**：采用三平面Transformer（Triplane Transformer），将3D空间分解为三个轴对齐平面，通过交叉图像和交叉平面可变形注意力聚合证据；使用连续正弦位姿编码代替固定可学习嵌入以支持可变视图数。
  - **融合**：将代价体分支的高斯投影到三平面，促进跨分支信息交换，最终合并两组高斯。
- **训练策略**：
  - **随机视图子采样**：在时间容器内随机采样输入立体对数量（1～K）和位置，迫使网络学习视图不变几何。
  - **连续位姿编码**：对相机位姿进行正弦编码，与随机子采样共同实现输入不变性。
  - **伪视图混合训练**：训练时以概率p将真实立体对替换为当前模型渲染的伪立体对，弥合训练-推理分布差异。
- **扩散增强渐进推理**：微调SD-Turbo的一步扩散模型（参考DIFIX3D+），用于增强渲染的伪视图，抑制伪影并保持视图一致性。
- **损失函数**：包含渲染RGB的L2+LPIPS损失、渲染深度L1损失、置信度回归损失，以及深度估计头损失。

## 3. 实验设计
- **数据集**：KITTI-360（城市驾驶数据集），包含校正立体图像和稀疏激光雷达数据。预处理：将序列分割为距离开头8米的**时间容器（bin）**，每个bin中第一帧立体对作为输入，其余帧（中心、最后一帧）作为新视角监督和评估。共54,862个bin，划分49,377训练/5,485评估。
- **评价指标**：RGB质量：PSNR、SSIM；几何精度：渲染深度的绝对相对误差（AbsRel）和平方相对误差（SqRel）。在bin内第一帧、中心帧、最后一帧及平均值上报告。
- **对比方法**：PixelSplat [4]、MVSplat [5]、OmniScene [2]、DepthSplat [3]——均为近期前馈3DGS基线。

## 4. 资源与算力
- **硬件**：4块NVIDIA A6000 GPU。
- **训练参数**：使用AdamW优化器（betas=0.9,0.99），初始学习率8e-5，余弦带warmup（1k步），训练30个epoch，batch size为4。模型运行分辨率112×544。
- **训练时长**：论文未明确给出总时间，但提及训练了1,450,000次迭代（对应30个epoch + warmup）。使用梯度检查点以节约显存。

## 5. 实验数量与充分性
- **消融实验**（表I）：在序列0000上进行，共9,207训练bin/1,023评估bin。验证了四个因素：
  1. 分支隔离（仅代价体 vs 仅3D体积 vs 融合）
  2. 随机视图子采样
  3. 扩散增强渐进推理（StereoSplat+）
  共5种配置，每种报告了四个位姿下的PSNR/SSIM，实验数量充分。
- **主要性能对比**（表II）：全验证集上对比4种基线，报告平均PSNR/SSIM/深度误差。统计差异明显。
- **定性比较**：图4（消融可视化）、图5（对比方法RGB/深度）、图6（鸟瞰外推）。
- **额外应用**：图7展示渐进融合用于大规模3D重建（未定量）。
- **评估客观性**：采用标准指标和公开数据集，对比方法使用官方代码或复现，实验设计公平。但仅在单个数据集KITTI-360上验证，缺乏跨数据集泛化实验（如Waymo、nuScenes），且未与基于优化的3DGS或NeRF方法对比（仅前馈基线）。

## 6. 主要结论与发现
- **StereoSplat（无扩散）** 已超越所有前馈基线，PSNR提升1.38 dB（vs PixelSplat 20.42 vs 19.04），深度误差降低明显（AbsRel 0.071 vs 0.094）。
- **StereoSplat+** 进一步改善：平均PSNR 20.76，SSIM 0.69，比StereoSplat提升0.34 dB / 0.02 SSIM。
- 在遮挡区域和强外推（如远视角、鸟瞰图）下，扩散增强渐进推理显著改善了场景完整性和几何一致性，减少浮动伪影和失真。
- 随机视图子采样和连续位姿编码实现了输入不变性，模型能够随输入立体对数量增加而自然提升质量。

## 7. 优点
- **输入不变性**：支持可变数量的立体对输入，无需固定视角；训练策略使网络对视图数目和位姿变化鲁棒。
- **高效前馈**：无需测试时每个场景的优化，一步扩散增强不破坏实时性。
- **联合深度-3DGS估计**：无需外部深度模型，成本体和三平面协同提供补全和几何约束。
- **渐进推理**：通过扩散增强伪视图重用模型，有效缓解单立体对覆盖不足，且仅需一次迭代。
- **消融实验系统**：清晰验证每个组件的贡献（双分支、随机子采样、扩散增强）。

## 8. 不足与局限
- **扩散增强的潜在副作用**：一步扩散模型可能偶尔引入人工痕迹，并在多次渐进更新中累积误差，论文未研究多次迭代的情况。
- **动态对象未建模**：对于移动车辆、行人等，模型没有显式处理，可能导致时序不一致，影响大规模增量融合重建。
- **仅评估KITTI-360**：缺乏在更复杂或室内场景数据集（如ScanNet、Replica、Mip-NeRF 360）上的验证，泛化性未证明。
- **未对比基于优化的方法**：虽然侧重前馈，但与优化型3DGS（例如原始3DGS、InstantSplat）在同等输入下比较会更有说服力。
- **计算成本**：虽然前馈快，但双分支结构（尤其是三平面Transformer）可能内存消耗较高，论文未提供推理时间或参数量的分析。

（完）
