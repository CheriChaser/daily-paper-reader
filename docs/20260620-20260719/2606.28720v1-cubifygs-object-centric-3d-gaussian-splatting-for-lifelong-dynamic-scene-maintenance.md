---
title: "CubifyGS: Object-Centric 3D Gaussian Splatting for Lifelong Dynamic Scene Maintenance"
title_zh: CubifyGS：面向对象的3D高斯泼溅用于终身动态场景维护
authors: "Bohan Ren, Dianyi Yang, Shiyang Liu, Yu Gao, Jiadong Tang, Zhilin Lai, Yi Yang, Mengyin Fu"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28720v1"
tags: ["query:dgs-recon"]
score: 9.0
evidence: 面向动态场景维护的物体级3DGS
tldr: 针对机器人长期场景中物体重新排列导致的伪影和低效问题，提出对象级3D高斯映射框架CubifyGS。将可移动实例建模为可重用高斯资产，通过资产检索、刚性变换和显式剪枝实现地图更新，而非从头重建。进一步设计事件触发自适应优化策略，聚焦修正受影响的区域。在新建的高保真动态基准上，该方法显著抑制伪影并提升维护效率，优于代表性基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28720v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 884, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28720v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1816, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28720v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1801, \"height\": 906, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28720v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28720v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 875, \"height\": 706, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28720v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28720v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 894, \"height\": 340, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28720v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1832, \"height\": 290, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28720v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 895, \"height\": 273, \"label\": \"Table\"}]"
motivation: 现有3DGS方法在物体重排场景中需从头优化，导致持久伪影与慢恢复，缺乏主动资产重用机制。
method: 将可移动实例作为高斯资产管理，检测出现/消失后通过资产检索、刚体变换与显式剪枝更新地图，并采用事件触发自适应优化修复几何空洞与光色差异。
result: 在新建动态基准上，CubifyGS在伪影抑制和维护效率上均优于当前典型基线方法。
conclusion: 所提框架实现了从被动重优化到主动资产管理的范式转换，有效支撑动态场景的长期维护。
---

## 摘要
在刚性物体重排下的终身场景映射仍然是机器人领域的一个基本挑战。虽然3D高斯泼溅（3DGS）能够实现高保真建模，但基元级别的更新往往会导致持续的鬼影和缓慢的恢复。我们提出了CubifyGS，一个对象级别的映射框架，将动态维护从被动重新优化转变为主动资产管理。CubifyGS将可移动实例建模为可重复使用的高斯资产，检测物体的出现和消失，并通过资产检索、刚性变换和显式修剪来更新地图，而非从头重建。为了解决这种编辑后的几何空洞和局部光度不匹配问题，我们进一步提出了一种事件触发的自适应优化策略，将计算集中在受影响区域。我们在一个新构建的高保真动态基准上验证了我们的方法，结果表明，在评估的物体重排设置中，CubifyGS在伪影抑制和维护效率方面优于代表性的可复现基线。

## Abstract
Lifelong scene mapping under rigid object rearrangement remains a fundamental challenge in robotics. While 3D Gaussian Splatting (3DGS) enables high-fidelity modeling, primitive-level updates often cause persistent ghosting and slow recovery. We propose CubifyGS, an object-level mapping framework that shifts dynamic maintenance from passive re-optimization to active asset management. CubifyGS models movable instances as reusable Gaussian assets, detects object appearance and disappearance, and updates maps through asset retrieval, rigid transformation, and explicit pruning rather than reconstruction from scratch. To address geometric voids and local photometric mismatch after such edits, we further propose an event-triggered adaptive optimization strategy that focuses computation on affected regions. We validate our approach on a newly constructed high-fidelity dynamic benchmark, demonstrating that CubifyGS improves artifact suppression and maintenance efficiency over representative reproducible baselines in the evaluated object-rearrangement setting.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在机器人长期运行的室内动态场景中，刚性物体频繁发生重排（消失、出现、移动），传统的3D高斯泼溅（3DGS）映射方法假定静态环境，在遇到物体重排时采用“擦除-重建”的被动梯度优化范式，导致**持久鬼影伪影**和**计算冗余**（需要从零重新收敛），严重影响地图一致性和下游任务。
- **研究动机**：室内动态通常是**物体稀疏且可复用**的，因此提出将维护从被动重新优化转向主动资产管理——将可移动实例建模为可重用高斯资产，通过显式修剪、检索和变换实现高效更新。
- **整体含义**：CubifyGS是一种面向对象的终身动态映射框架，专门针对刚性物体重排场景，旨在实现快速、无伪影、高保真的地图维护。

## 2. 论文提出的方法论
### 2.1 核心思想
- 将场景表示为**统一的对象中心高斯表示**（Object-Centric Gaussian Representation），包括：
  - **活动场景列表**：静态背景 + 动态前景实例，每个实例包含几何（高斯基元）、语义（多视角DINO特征银行）、空间边界框（重力对齐3D框）。
  - **全局资产库**：存储标准坐标系下的对象模板，通过刚体变换实现即时复用。
- 将地图维护从“被动重新优化”转变为**主动资产管理**：检测物体消失后显式剪枝（去除鬼影），检测新物体后从资产库检索并对齐复用资产，避免从零优化。

### 2.2 关键技术细节
- **时空动态感知模块**：
  - **层次化跟踪与初始化**：基于BoxFusion范式，通过3D几何IoU和2D投影IoU进行数据关联，未匹配的提议初始化为新候选物体。
  - **消失物体检测**：通过**射线投射占据分析**（Ray-Casted Occupancy Analysis）对每个物体发送射线，根据深度比较判断“被遮挡/命中/自由”状态，利用自由射线判定空间空置，结合时序衰减规则更新存在概率，低于阈值则标记为“已消失”。
- **终身场景维护策略**：
  - **概率性鬼影剪枝**：显式移除消失物体边界框内所有高斯基元。
  - **语义感知资产检索与对齐**：计算当前物体视觉特征与资产库中多视角特征银行的余弦相似度，选择最佳匹配资产及姿态初始化；然后通过**可微相机跟踪**（冻结高斯参数，仅优化相对相机位姿）进行精细对齐，近似梯度仅利用投影几何项。
  - **候选验证与资产注册**：满足几何覆盖度（≥视角阈值）和梯度稳定度（梯度移动平均≤ε）的对象才提升至资产库。
- **事件触发自适应优化**：
  - 生成**感兴趣区域（ROI）掩码**：将变化物体的3D边界框投影到图像平面。
  - **聚焦驱动梯度下降**：在损失函数中加入空间权重图，对ROI内像素赋予高权重γ_focus >> 1，使梯度流集中在动态区域，加速背景修补和资产微调，同时防止静态背景遗忘。

### 2.3 公式与算法流程（文字说明）
- 物体表示：Oi = (Gi, Fi, Bi)，Gi为高斯集合，Fi为M个固定视角的DINO特征集，Bi为3D边界框。
- 资产实例化变换：μ_world = R_i μ_canonical + t_i，Σ_world = R_i Σ_canonical R_i^T。
- 射线状态分类：根据观测深度与边界框相交区间[ t_in, t_out ]，判断为Occluded、Hit、Free。
- 存在概率更新：命中时增加，自由时严重衰减，其他情况下指数衰减。
- 检索匹配：(A*, k*) = argmax cos(f_new, f_Aj^(k))。
- 精细对齐梯度近似：∂L_pho / ∂T ≈ Σ (∂L_pho/∂C)(∂C/∂α_i)(∂α_i/∂m_i)(∂m_i/∂T)。
- 自适应损失：L_adapt = Σ_u Ω(u)·[(1-λ)L1(u)+λL_D-SSIM(u)]，其中Ω在ROI内为γ_focus，其他为1。

## 3. 实验设计
### 3.1 数据集/场景
- **自建高保真动态基准**（Blender合成）：包含四个室内场景——Office-1（29个物体，3个动态）、Bedroom-1（23个物体，6个动态）、Livingroom-1（56个物体，11个动态）、Kitchen-1（42个物体，14个动态），每个序列1000帧，提供地面真值位姿、深度、实例掩码和3D边界框。
- **真实数据**：Bonn kidnapping box2（1279帧，约30个物体，1个动态），用于验证迁移能力。

### 3.2 对比方法
- 静态3DGS映射基线：MonoGS、GS-ICP SLAM、SplaTAM、WildGS-SLAM（动态基线）。所有方法均使用地面真值位姿以解耦跟踪漂移。

### 3.3 评估指标
- **渲染质量**：动态ROI上的PSNR、SSIM、LPIPS（每30次优化迭代采样，最后取事件内平均）。
- **感知精度**：3D mAP@25/50和AR@25/50（遵循CubifyAnything协议）。
- **映射效率**：平均FPS。

## 4. 资源与算力
- **硬件**：Intel i9-14900KF CPU + 单张NVIDIA RTX 4090 GPU。
- **软件**：基于MonoGS代码库（PyTorch）。
- **训练时长**：未明确给出完整训练时长，但提到精细对齐使用两阶段Adam调度（共1000次迭代），系统级评估中每个重排事件后优化10秒。
- **运行时效率**：CubifyGS达到约20 FPS，对比WildGS-SLAM的<0.5 FPS（超过40倍加速）。

## 5. 实验数量与充分性
### 5.1 实验组数
- **系统级对比**：5个序列（4合成+1真实），每个序列报告PSNR/SSIM/LPIPS三个指标。
- **动态感知验证**：4个合成场景，报告AP25/AP50/AR25/AR50，与单帧基线CubifyAnything对比。
- **检索与对齐分析**：3个方面——语义可区分性（t-SNE可视化+余弦相似度曲线）、粗对齐精度（±15°误差）、精对齐鲁棒性（5°~50°扰动下收敛成功率）。
- **消融实验**：2个最挑战场景（Livingroom-1、Kitchen-1），分别消融“聚焦驱动优化”和“全局资产库”，共6组条件。

### 5.2 充分性与公平性
- **充分性**：实验覆盖了不同物体密度（3~14动态物体）、合成与真实场景，指标全面（渲染+感知+效率），消融验证了核心组件贡献。检索对齐分析通过定量和可视化证明了模块的有效性。整体实验设计合理，结论有据。
- **公平性**：所有基线均使用地面真值位姿，避免跟踪漂移影响；评估聚焦动态ROI而非全图，消除静态背景偏差；对比方法均为可复现的公开方法。

## 6. 论文的主要结论与发现
- CubifyGS在动态ROI上PSNR平均提升35.83%（相对最强基线），在Bedroom-1上提升7.58 dB。
- 显式资产剪枝与检索避免了鬼影和慢收敛，使伪影抑制和维护效率显著优于所有基线。
- 事件触发自适应优化使损失权重集中在动态区域，确保背景快速修补和资产微调稳定。
- 3D物体定位精度（AP25高达65.2%）大幅优于单帧基线（26.7%），证明多视图时序关联对感知鲁棒性关键。
- 检索-对齐模块：粗对齐误差控制在±15°以内，精对齐在25°以内扰动下成功率达近100%，保证了资产复用的可靠性。

## 7. 优点
- **范式创新**：将维护从被动优化转为主动资产管理，是3DGS动态映射领域的新思路。
- **模块化设计**：感知、维护、优化各模块解耦清晰，可独立替换或增强。
- **效率优势**：显式剪枝+资产复用避免了耗时重新优化，20 FPS的推理速度适用于实时机器人系统。
- **完备的实验验证**：自建高保真基准填补了缺乏物体重排场景的空白；消融实验量化了每个组件的贡献；检索对齐分析提供了细粒度模块评估。
- **适配真实场景**：在真实数据集Bonn box2上验证了迁移能力。
- **开源基础**：基于公开代码库MonoGS，易于复现和扩展。

## 8. 不足与局限
- **场景假设局限**：仅考虑刚性物体重排，且资产库中物体被固定为模板，无法处理非刚性变形、物体材质变化或完全新颖的物体（未注册到资产库）。
- **自建基准的生态限制**：主要使用合成数据，真实世界的低光照、传感器噪声、遮挡等复杂情况未充分覆盖（仅一个真实序列）。
- **资产库质量依赖初始训练**：资产注册需要满足多视角覆盖和梯度稳定条件，在稀疏视线下可能失败，限制了首见物体的快速上线。
- **事件检测依赖阈值**：消失检测中的阈值τ_free、τ_occ等需手动设定，不同场景可能需要调整；精细对齐的迭代次数固定（1000步），可能浪费或不足。
- **未探讨增量资产更新**：论文提到当前资产作为固定模板复用，未来需要增量更新和跨会话合并——这限制了长期自主演化能力。
- **实验未涉及跟踪与定位性能**：所有实验使用地面真值位姿，未评估CubifyGS在完整SLAM系统中对跟踪精度的影响。

（完）
