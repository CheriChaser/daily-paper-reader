---
title: "SemDynReg: Semantics-Guided Deformation Regularization for Dynamic 3D Gaussian Splatting"
title_zh: "SemDynReg: 语义引导的动态三维高斯溅射变形正则化"
authors: "Ruitao Chen, Mozhang Guo, Jinge Li"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28656v1"
tags: ["query:dgs-recon"]
score: 9.0
evidence: 提出语义引导的变形正则化用于动态3D高斯溅射
tldr: 动态场景渲染中，基于变形场的3D高斯泼溅方法缺乏对象级建模，导致变形不一致和对象耦合。本文提出SemDynReg框架，利用SAM提取语义掩码并构建对象ID图，对每个对象的高斯位置、尺度、旋转参数施加一致性正则化。实验在多个数据集上取得更高PSNR和SSIM，更低LPIPS，显著提升对象级变形一致性。贡献在于将语义线索显式引入动态3DGS，实现高效且一致的对象变形正则化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28656v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1862, \"height\": 1093, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28656v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1881, \"height\": 1136, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28656v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1882, \"height\": 1131, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28656v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 932, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28656v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 932, \"height\": 397, \"label\": \"Table\"}]"
motivation: 解决现有动态3DGS方法缺乏对象级建模导致的变形不一致和对象间耦合问题。
method: 利用SAM提取语义特征并构建对象ID图，对每个对象的高斯变形参数（位置、尺度、旋转）施加一致性正则化。
result: 在动态场景渲染中，相比基线方法PSNR和SSIM更高，LPIPS更低，对象级变形一致性更优。
conclusion: 语义引导的变形正则化有效提升了动态3DGS的渲染质量和变形一致性。
---

## 摘要
可变形三维高斯溅射（3DGS）已成为一种在广泛3D应用中渲染动态场景的高效方法。然而，现有的基于变形场的方法大多缺乏显式的对象级建模，常导致单个对象内的高斯变形不一致以及不同对象之间的非必要耦合。为解决这一局限，我们引入了一个语义引导的框架，在对象级别强制执行动态正则化，旨在实现空间一致的对象级变形。具体而言，我们首先使用分割一切模型（SAM）提取分割掩码，并从输入图像中导出语义特征。然后通过预定义对象字典的特征相关性匹配构建对象ID图。在该对象ID图的引导下，我们为每个对象识别像素级的前k个贡献高斯，并对其变形参数（包括位置、尺度和旋转）施加一致性正则化。与先前在无显式对象级约束下学习变形场的方法不同，我们的方法融入语义线索以在对象级别引导变形行为。实验结果表明，我们的语义感知正则化提升了对象级变形一致性，并在渲染质量上优于基线方法，在动态3DGS渲染中实现了更高的PSNR和SSIM以及更低的LPIPS。我们的项目页面位于 https://dyn-reg-3dgs.github.io/。

## Abstract
Deformable 3D Gaussian Splatting (3DGS) has emerged as an efficient approach for rendering dynamic scenes in a wide range of 3D applications. However, existing deformation field-based approaches largely lack explicit object-level modeling, often resulting in inconsistent Gaussian deformations within individual objects and unwanted coupling between different objects. To address this limitation, we introduce a semantics-guided framework that enforces dynamic regularization at the object level, aiming to achieve spatially consistent object-wise deformation. Specifically, we first extract segmentation masks using the Segment Anything Model (SAM) and derive semantic features from input images. An object-ID map is then constructed via feature relevance matching with a predefined object dictionary. Guided by this object-ID map, we identify the pixel-wise top-k contributing Gaussians for each object and impose consistency regularization on their deformation parameters, including position, scale, and rotation. Unlike prior methods that learn deformation fields without explicit object-level constraints, our approach incorporates semantic cues to guide deformation behavior at the object level. Experimental results demonstrate that our semantics-aware regularization improves object-level deformation consistency and outperforms baseline methods in rendering quality, achieving higher PSNR and SSIM and lower LPIPS in dynamic 3DGS rendering. Our project page is available at https://dyn-reg-3dgs.github.io/.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有基于变形场的动态3D高斯溅射（Deformable 3DGS）方法在渲染动态场景时缺乏显式的**对象级建模**，导致同一对象内不同高斯的变形不一致，以及不同对象之间的非必要耦合，进而产生视觉伪影（如运动模糊、结构扭曲）。
- **动机**：为了获得更真实、物理合理的动态渲染，需要利用语义线索将高斯按对象分组，并强制同一对象内的高斯变形保持一致。
- **整体含义**：本文提出 **SemDynReg**——一个**语义引导的变形正则化框架**，首次将对象级语义约束显式引入动态3DGS的变形学习过程，提升动态场景渲染质量和对象运动一致性。

## 2. 方法论

### 2.1 核心思想
- 通过分割和大模型获取图像的语义信息，构建**对象ID图**；然后对每个对象关联的高斯，识别其**像素级top-k贡献高斯**；最后对这些高斯的变形参数（位置δx、旋转δr、尺度δs）施加**一致性正则化**，使同一对象内的高斯变形趋近于该对象的平均变形。

### 2.2 关键技术细节（流程文字说明）

1. **语义特征提取**：
   - 使用 **SAM** 从输入图像提取非重叠的候选分割掩码 M。
   - 对每个掩码区域，使用 **CLIP**（OpenCLIP ViT-B/16）编码为512维语义特征 f_j，得到稠密语义特征图 F ∈ R^{H×W×512}。

2. **对象ID图构建**：
   - 预定义对象字典 D = {t_i}（如“白色SUV”“黄色轿车”），每个文本提示用 CLIP 文本编码器得到嵌入 e_i。
   - 对每个像素 v，计算其特征 F(v) 与每个对象嵌入 e_i 的归一化相关性得分 r_i(v)（基于上下文提示集 C 的 min-over-context 归一化）。
   - 若最大得分 > 阈值 τ，则赋予对象标签；否则视为背景/无关标签，得到对象ID图 ID ∈ {0,...,C-1,背景}。

3. **Top-k高斯选择与对象级分组**：
   - 在渲染过程中，每个像素 p 有贡献权重 w_i(p) = T_i α_i（来自alpha合成）。
   - 对每个像素选取贡献权重最大的 **k 个高斯**（索引集合 K(p)）。
   - 对每个对象 o，聚合所有属于该对象的像素所对应的 top-k 高斯，得到对象高斯集合 G_o。

4. **变形正则化**：
   - 对每个对象 o，计算其高斯集合 G_o 内变形参数（δx, δr, δs）的均值。
   - 正则化损失为各高斯参数与均值的**均方误差（MSE）**，分别用 λ_x, λ_r, λ_s 加权。
   - 若对象平均变形大小低于阈值 τ_d，则视为静态对象，跳过正则化。
   - 总损失 L = L_rgb + λ L_reg，在训练后期（10k次迭代后）激活。
   - 该方法默认适用于近似刚体运动（如车辆），对非刚体可扩展到部件级。

## 3. 实验设计

- **数据集**：使用**私有数据集**，包含两个受控动态场景（各含三个微型玩具车，一个运动、两个静止），使用移动单目相机拍摄，时间步6个。
- **Benchmark**：基于可变形3DGS的基线方法（不施加任何语义约束），在同一管道上对比。
- **对比方法**：仅与**自身基线（无正则化）** 比较，未与其他语义引导动态GS方法（如4D LangSplat、CoDa-4DGS等）直接对比。
- **评价指标**：PSNR（↑）、SSIM（↑）、LPIPS（↓），在**放大后的运动对象区域**按时间步分别计算并取平均。

## 4. 资源与算力

- **GPU**：单张 **NVIDIA RTX 3090**，显存占用约 **4GB**。
- **训练时长**：每个场景约 **25分钟**（40,000次迭代）。
- 文中明确给出了这些信息。

## 5. 实验数量与充分性

- **实验数量**：仅包含**两个场景**，每个场景6个时间步。定量结果见表I、表II。定性结果见图2、图3。
- **充分性分析**：
  - 优点：包含了定量（三个指标）和定性（渲染图、对象ID图、放大细节）评估，直观可信。
  - 不足：
    - **无消融实验**：未研究top-k中k值选取、λ强度、阈值τ等超参数影响。
    - **无公开基准**：私有数据集不便于复现和公平对比，缺乏与现有语义引导动态方法的比较。
    - **场景单一**：仅含近似刚体的车辆，未在非刚体（行人、动物）或复杂多物体场景验证。
    - **静态对象验证不足**：仅声称静态对象无影响，但未给出定量比较。
- **公平性**：基线和本文方法使用相同初始化和训练流程，仅差一个正则项，对比公平。

## 6. 主要结论与发现

- 语义引导的对象级变形正则化**显著提升了运动对象的渲染质量**：在场景1中，PSNR从26.459dB提升至29.615dB，SSIM从0.841提升至0.888，LPIPS从0.141降至0.109；场景2类似。
- 视觉上，运动对象（白色SUV、黄色轿车）边界更清晰、细节更丰富、运动伪影更少。
- 静态对象不受负面影响，表明选择性正则化有效。

## 7. 优点

- **创新性**：首次将**语义信息显式用于引导动态3DGS的变形正则化**，而非仅用于场景理解或编辑。
- **方法简洁有效**：在现有可变形3DGS上添加一个轻量正则项，易于集成，无需修改网络结构。
- **鲁棒性**：采用**top-k高斯而非单一高斯**，更全面覆盖对象关联高斯，提升正则化稳健性。
- **实用性强**：如果下游任务已使用SAM/CLIP（如语义编辑），则几乎无额外成本。
- **实验结果清晰**：定性与定量一致，视觉改善明显。

## 8. 不足与局限

- **数据集局限**：仅使用私有小数据集（两个场景），无公开标准基准（如Waymo、KITTI动态序列），结果泛化性存疑。
- **缺乏消融研究**：未分析k值、λ、阈值等超参数影响，也无法确定最优配置。
- **缺乏与现有方法的比较**：未对比4D LangSplat、CoDa-4DGS、AD-GS等同样使用语义或对象信息的动态GS方法，难以评估相对优势。
- **仅处理刚体对象**：对非刚体（人、动物）未验证，作者虽提出扩展到部件级但并未实现。
- **语义获取依赖额外模型**：SAM和CLIP引入推理时间开销（虽可复用），论文未量化这部分时间。
- **实验指标仅在放大对象区域计算**，未报告全图指标，可能存在选择性偏袒。

（完）
